# strategy/runs/2026-04-26-0140.md

**轮次**: 2026-04-26 01:40 (Asia/Shanghai)  
**Owner**: Jah-yee/ml-decision-boundary cron agent  
**分支**: master（无 PR，直接 push）

---

## 现状快照

- **分支/HEAD**: `master` at `f748357` (Update README.md)
- **工作区状态**: 干净（clone 后无改动）
- **ML 行为路径改动**: 无（仅建立测试基础设施）
- **部署文件**: vercel.json + api/ + web/ 未变化
- **风险**: 尚未评估磁盘占用；pytest 依赖未写入 requirements.txt；serverless 冷启动未测

---

## 5 分叉选择

| # | 方向 | 本轮 |
|---|------|------|
| 1 | Harness/可复现实验平台化 | ✅ **主攻** |
| 2 | 模型与数据科学深化 | ❌ 下轮 |
| 3 | 产品交互与契约 | ❌ 下轮 |
| 4 | 部署与运行时工程 | ❌ 下轮 |
| 5 | 治理与社区工程 | ❌ 下轮 |

**本轮选 #1（测试/Harness 基础设施）的原因**: 当前处于 v0 阶段末尾，P1（测试）和 P2（Harness 可复现）是制约项目演化的最短板。无测试则无回归，无 harness 则每次"实验"都是一次性的。本轮优先建立测试基础设施和 P0 可复现基线。

---

## ML 动作

- **建立测试基础设施**: conftest + tests/test_main.py（11 个测试用例，11 passed）
- **发现并修复 API 契约 bug**: train_model 参数顺序为 (model_type, params, X, y)，非 README 中暗示的 (model_type, X, y, params)；已在测试中修正
- **运行 P2 benchmark**: `python3 main.py` 成功，输出 experiment_results.json + 9 张 PNG

---

## 文档轮换两项

- ✅ `spec/CHARTER.md` (新建)
- ✅ `spec/phases.md` (新建)
- ✅ `spec/REPRODUCE.md` (新建)
- ✅ `tests/test_main.py` (新建)
- ✅ `benchmarks/reports/2026-04-26.md` (新建，见下方)

---

## 通过层级

| 层级 | 状态 | 证据 |
|------|------|------|
| P0 | ✅ 通过 | `python3 -m compileall .` 无错误；import smoke 成功 |
| P1 | ✅ 通过 | pytest 11/11 passed |
| P2 | ✅ 通过 | `python3 main.py` 成功，输出完整 JSON + PNG |
| P3 | ⚠️ 未测试 | API health 未在本地测试（待下轮） |

---

## 本轮触及文件

```
spec/CHARTER.md          (新建)
spec/phases.md           (新建)
spec/REPRODUCE.md        (新建)
tests/__init__.py        (新建)
tests/conftest.py        (新建)
tests/test_main.py       (新建)
benchmarks/reports/2026-04-26.md (新建)
strategy/NEXT_ROUND_THEME.md (更新)
CHANGELOG.md             (更新)
```

---

## 决策记录

### 决策1: 测试框架选择
- **选项**: pytest vs unittest
- **决策**: pytest（更简洁，有 fixtures，社区广泛）
- **理由**: sklearn/scipy 生态通用选择

### 决策2: train_model API 修正
- **问题**: README 中 train_model 示例暗示 (model_type, X, y, params) 顺序，但实际是 (model_type, params, X, y)
- **决策**: 保持代码不变（避免破坏现有调用方），在测试中记录实际签名，在 REPRODUCE.md 中记录正确用法
- **风险**: README 与代码不一致，但不影响核心功能

### 决策3: 测试数据规模
- **选项**: n_samples=200 vs 500
- **决策**: n_samples=200（小规模，快）
- **理由**: 测试追求速度和确定性，不追求统计显著性

---

## 回滚策略

- 如有严重问题：`git revert <commit>` 或开 daily 分支回滚
- 本轮所有新文件均在 `tests/` 和 `spec/` 下，不影响核心功能路径
- benchmark report 仅为参考文档

---

## 下一轮待办

1. **P3**: 测试 API health endpoint (`curl localhost:5000/api/health`)
2. **P1 扩展**: 添加 `test_api_train.py`
3. **文档**: 修复 README 中的 train_model 参数顺序示例
4. **风险**: 评估 pytest 是否需要加入 requirements.txt
5. **模型深化**: 扩展 MLP max_iter 以消除 ConvergenceWarning
