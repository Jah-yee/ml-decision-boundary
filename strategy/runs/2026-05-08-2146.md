# 2026-05-08 Evening Session Run Log

**时间**: 2026-05-08 21:46 CST
**执行人**: 太子 (Jah-yee/ml-decision-boundary cron agent)
**分支**: fix/report-html-ts-bug
**主题入口**: v3 DoD 深度维护（第2项：新数据集/新模型支持评估）

---

## 背景

晨间场已确认 ADR-0003 完成（v3 DoD 首项）。本轮为 evening 场，执行深度扫描 + v3 DoD 次项评估。

---

## 深度扫描发现

### 高优先级
1. **CI P2 step failing** — `--smoke` vs `--quick` flag 不匹配 — **修复完成**（PR#23）

### 中优先级
1. **report_html.py API 变化** — `generate_html_report()` 函数名不存在，实际为 `generate_report(reports_dir, output_dir)`；README/CHANGELOG 可能需要更新说明

### 低优先级
1. research/ 有2个文档（2026-04-28 和 2026-05-01），未发现新的待归档发现

---

## 本轮工作（1个问题域，3个commit，专项深挖模式）

### 问题域：CI P2 benchmark step 修复（分叉#5：治理与社区工程）

**问题描述**: CI workflow 中调用 `benchmarks/run.py --smoke`，但实际 flag 为 `--quick`，导致所有 CI run 在 P2 step 失败。

**影响**: 10个连续 CI run 全部失败（benchmark + quality-gates jobs）

---

### commit 1: fix(ci): use --quick instead of --smoke for benchmark smoke test
- 新建分支 fix/ci-smoke-flag
- 修改 .github/workflows/ci.yml P2 step：`--smoke` → `--quick`
- 创建 PR#23

### commit 2: docs(benchmarks): refresh 2026-05-08 reports with latest full-suite run
- 刷新 benchmarks/reports/2026-05-08.{html,json,md,*_trend.png}
- 来自最新 `python3 benchmarks/run.py --report` 执行（52实验，45通过/7失败，avg accuracy 0.8237）

### commit 3: docs(benchmarks): add depth sweep 2026-05-08 (24 experiments, 22 passed)
- 新增 benchmarks/reports/depth_sweep_2026-05-08.json + .md
- 24 experiments across depth∈{1,2,3,5,10,None} × 4 datasets
- 22 passed, 2 failed（circles@depth=1, xor@depth=1/2/3）

---

## 通过层级

- **P0**: ✅ pass（python3 -m compileall 无错误）
- **P1**: ✅ pass（100 tests collected, pytest 全部通过）
- **P2**: ✅ pass（`benchmarks/run.py --quick`: Accuracy 0.7900 >= Threshold 0.7000）

---

## 本轮完成

- [x] CI P2 step `--smoke` → `--quick` 修复（PR#23）
- [x] 2026-05-08 benchmark 报告刷新（full-suite, 52实验）
- [x] Depth sweep 2026-05-08 归档（24实验）
- [x] Push 到 fix/report-html-ts-bug

---

## 下轮待办

- [ ] 验证 PR#23 CI 是否通过（fix/ci-smoke-flag 分支）
- [ ] 新数据集支持评估（v3 DoD 第2项候选）
- [ ] 新模型支持评估（v3 DoD 第2项候选）
- [ ] merge PR#23 后关闭 fix/ci-smoke-flag 分支

---

## PR

| # | Title | Branch | Status |
|---|-------|--------|--------|
| 22 | feat(benchmarks): HTML report generator + daily reports | fix/report-html-ts-bug | OPEN（CI 待修复） |
| 23 | fix(ci): use --quick instead of --smoke for benchmark smoke test | fix/ci-smoke-flag | OPEN（本轮创建） |

---

## CI 问题分析

**症状**: CI run #25533016965 及之前10个 run，两个 job 都在 step "Install dependencies" 失败（exit code 1）

**根因**: 不是依赖安装失败，而是 benchmark job 的 "P2 — Benchmark smoke" step 报错 `unrecognized arguments: --smoke`，导致 shell 退出码为1。但 GitHub Actions UI 误报为 "Install dependencies" step failure（因为它是最底层 failure step）。

**修复**: `.github/workflows/ci.yml` P2 step `python3 benchmarks/run.py --smoke` → `python3 benchmarks/run.py --quick`

---

## 时间分配

```
扫描 + 分析：20分钟
CI 问题定位：10分钟
修复方案实施（fix/ci-smoke-flag）：10分钟
报告刷新（full-suite + depth sweep）：25分钟
Push + PR 创建：5分钟
收尾：10分钟
总计：~80分钟
```

---

**状态**: ✅ 已完成
**下次更新**: 下一轮 cron 执行后
