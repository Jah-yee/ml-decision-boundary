# Run Log — 2026-05-20 Morning Session

**Agent**: taizi (Owner 代理)
**Session**: ml-decision-boundary 每日闭环 Morning
**Time**: 2026-05-20 09:47 CST
**Theme**: v4 启动 · REPRODUCE.md 全面更新 · Tree depth CI 集成

---

## 执行摘要

v4 首轮：完成 REPRODUCE.md 全面更新 + depth-sweep CI 集成，作为 v4 DoD 前两项的实质性进展。

### 核心交付

| 交付物 | 状态 | 说明 |
|--------|------|------|
| REPRODUCE.md 全面重写 | ✅ 已 commit+push | 8777 bytes, v4 主题入口 |
| .github/workflows/ci.yml 更新 | ✅ 已 commit+push | 新增 depth-sweep job |
| spec/phases.md v4 DoD 更新 | ✅ 已 commit+push | 标注本轮进展 |
| P0/P1/P2 验证 | ✅ 通过 | compileall无错 + 100 tests |
| Full benchmark run | ✅ | 100 exp, 89 passed, 11 expected-fail |
| Tree depth sweep run | ✅ | 30 exp, 27 passed, depth数据完整 |

---

## P0/P1/P2 验证

```bash
# P0
python3 -m py_compile $(find . -name "*.py" ...)  # ✅ 无错误

# P1
pytest tests/ -q --tb=short  # ✅ 100 passed in 109.38s

# P2
python3 benchmarks/run.py --quick  # ✅ PASSED (0.79 vs 0.70)
python3 benchmarks/run.py           # ✅ 100 exp, 89 passed, 11 fail
python3 benchmarks/run.py --depth-sweep  # ✅ 30 exp, 27 passed
```

---

## 深度扫描发现

### 高优先级
1. **REPRODUCE.md 验证日期过期** → 已更新为 2026-05-20，全面重写
2. **Tree depth sensitivity CI 缺失** → 已添加 depth-sweep job 到 ci.yml

### 中优先级
3. **超参 sweep 阈值回归检测自动化** → 基础设施已存在(run.py)，尚未集成CI
4. **平台化后边界情况测试覆盖** → 待细化

### 低优先级
5. **ADR-0005 merge 状态** → 已确认 merge 到 master (8eb2682)

---

## 技术细节

### REPRODUCE.md 重写内容
- **平台架构**: `core/` 模块说明，所有入口统一
- **质量门**: P0/P1/P2 命令体系
- **Tree depth findings**: 详细数据矩阵（circles/moons peak at d=5, xor breakthrough at d≥5, blobs saturates at d=2）
- **完整预期结果表**: 所有模型×数据集组合，约89/100 pass
- **故障排查**: matplotlib backend, slow MLP, pytest hang, import errors
- **CI说明**: pipeline所有job说明

### CI depth-sweep job
- Trigger: every push (与 quality-gates/benchmark 平行)
- Command: `python3 benchmarks/run.py --depth-sweep`
- Artifact: `benchmarks/reports/depth_sweep_*.json`, 30天retention
- Timeout: 15 min
- Python 3.10, pip cache with requirements.lock

### Benchmark 当前状态
- Full suite: 100 exp (10 models × 5 datasets + GB/ET/AB variations)
- Pass rate: 89/100
- 11 expected failures: circles+linear-kernel SVM/LR, xor+Tree(d=3)/NB/AB, s_curve+KNN(d=3)
- Depth sweep: 30 exp, 27 passed
  - circles: 5/6 (d=3 threshold too strict, passes at d=5+)
  - moons: 6/6 all pass
  - blobs: 6/6 all pass
  - xor: 6/6 all pass (depth-specific thresholds work well)
  - s_curve: 4/6 (d=3,d=10 fail threshold at 0.50/0.60)

---

## 触及文件

```
REPRODUCE.md                  (重写, 8777 bytes)
.github/workflows/ci.yml     (新增 depth-sweep job)
spec/phases.md               (v4 DoD 更新)
```

### Git
```
commit 59358c8 docs(v4): REPRODUCE.md comprehensive update + depth-sweep CI integration
Branch: master → origin/master ✅
```

---

## 下轮待办

### 高优先级
1. **超参 sweep 阈值回归检测自动化** → 集成到 CI job (hyperparam-sweep)
2. **平台化后边界情况测试覆盖** → 扩展 test suite
3. **ADR-0005 merge 确认** → 已确认 ✅

### 中优先级
4. **benchmark 报告 HTML 化自动化** → 每次 full run 自动生成
5. **v4 DoD 剩余项细化**

---

## 本轮复盘

**成功：**
- REPRODUCE.md 从 ~2KB 扩展到 ~9KB，实质性内容大幅增加
- depth-sweep CI job 添加，规范化了每次 push 的 Tree depth 回归检测
- Full benchmark 可复现运行（100 exp, 89 pass, 11 fail）
- 所有P0/P1/P2通过

**限制：**
- hyperparam-sweep 尚未集成CI（本轮仅添加了 depth-sweep）
- boundary case 测试覆盖尚未开始

**下次改进：**
- 继续推进 v4 DoD：hyperparam sweep CI 集成
- 考虑 boundary case 测试覆盖的测试用例设计