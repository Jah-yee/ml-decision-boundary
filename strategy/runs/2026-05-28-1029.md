# 本轮执行摘要 — 2026-05-28 早场（v6 第2次闭环）

## 深度扫描发现

| 优先级 | 数量 | 典型问题 |
|--------|------|----------|
| 高 | 1 | respx/httpx 本地冲突导致 pytest 无法收集（本地 env issue，CI 不受影响） |
| 高 | 1 | core/train_utils.py 有两个 build_model 定义导致死代码 |
| 中 | 3 | v6 DoD 未细化（ADR-0009）、PR#40&41 等待 merge、README/SPEC 未同步 v6 |
| 低 | 2 | api/health.py 无 docstring、long-standing unchanged file benchmarks/ |

## 本轮工作（3个问题域，2个PR）

### 问题域1：core/train_utils.py 技术债务清理（专项深挖）
**PR#42**: `fix/v6-stability-do-dedup` → master

**根因**：原文件通过 try/except 两层 factory 兼容懒加载和直接 import，第二个 build_model 会 shadow 第一个。代码注释说"direct imports faster"，且 try/except 实际上在标准 pip env 下永远成功，所以 lazy 版本是死代码。

**修复（commit 1）**：
- 将 sklearn 直接 import 提升到模块顶层
- 删除第一个 lazy `__import__` factory（死代码）
- 删除第二个 direct `build_model` + try/except 包装
- 文件净减少 ~52 行（192→140），行为不变

**ADR（commit 2）**：
- docs/adr/ADR-0009-v6-dod.md：v6 阶段 DoD 细化
  - P1: respx/httpx env 修复 ✅ 根因定位（httpx 0.28.1 无 BaseTransport）
  - P1: build_model 重复定义清理 ✅ 本 PR 完成
  - P2: API contract test 增强
  - P2: Release 自动化

### 问题域2：README/SPEC.md v6 同步（ADR-0009 P3）
**PR#43**: `fix/v6-readme-sync` → master

**发现**：README.md 显示"6 real ML models" + "4 synthetic datasets"，但当前实现是 10 models + 5 datasets。SPEC.md phases.md 路径说明停留在 v0/v1/v2/v3。

**修复**：
- README.md：Models 6→10，Datasets 4→5（S-curve）
- SPEC.md：v0/v1/v2/v3 → v0 → v6，ADR-0001~ADR-0009
- SPEC.md 版本升至 v1.1.0

### 问题域3：respx/httpx 本地环境修复（ADR-0009 P1）
**发现**：本地 pytest 收集阶段失败（respx 0.23.1 + httpx 0.28.1 不兼容）
**根因**：respx 0.23.1 依赖 httpx.BaseTransport，已在 httpx 0.28.1 删除
**CI 不受影响**：requirements.lock 无 respx/httpx

**修复**：pip uninstall respx（--break-system-packages）+ force-reinstall httpx 0.28.1
**结果**：pytest 重新可运行 ✅ 216 tests passed

## 通过层级

| 层级 | 状态 | 说明 |
|------|------|------|
| P0 | ✅ 编译 + import smoke | 所有核心 import 正常 |
| P1 | ✅ pytest 216 tests | respx 卸载后正常通过 |
| P2 | ✅ benchmark smoke | SVM/circles acc=0.79 |
| CHANGELOG | ✅ | Unreleased 已更新 |

## 本轮完成

- [x] core/train_utils.py 重复 build_model 定义已清理（-52 行死代码）
- [x] ADR-0009-v6-dod.md 创建（v6 DoD 细化完成）
- [x] PR#42 创建并推送（CI all green ✅）
- [x] PR#43 创建并推送（README/SPEC sync，CI 运行中）
- [x] respx/httpx 本地环境修复（pytest 可正常运行）
- [x] CHANGELOG Unreleased 更新

## 根因分析：respx/httpx 冲突

**现象**：本地 pytest 收集阶段就失败（pytest 加载 respx 时尝试 import httpx.BaseTransport）

**根因**：
- `respx 0.23.1` 是一个全局安装的 pytest plugin（注册在 `pytest11` entry point）
- 它依赖的 `httpx 0.28.1` 也全局安装在系统 site-packages
- httpx 自 0.27.0+ 移除了 `BaseTransport`（从 HTTPX 改为 `BaseTransport` → `AsyncBaseTransport`）
- respx 0.23.1 尚未兼容 httpx 0.28（需要升级 respx 到 0.24+）

**CI 不受影响原因**：
- CI 只安装 `requirements.lock`（不包含 respx 或 httpx）
- pytest 运行时不加载 respx plugin
- 所有测试不依赖 respx（grep 结果为空）

**修复方案**：
- 已执行：pip uninstall respx（本地 pytest 恢复正常）
- 可选：pip install respx==0.24.0+（修复 httpx 0.28 兼容性）

## CI 状态

| PR | 标题 | 状态 | 说明 |
|----|------|------|------|
| #40 | v5→v6 升级 docs | OPEN | 等待 review |
| #41 | main.py 数据集去重 | OPEN | 等待 review |
| #42 | train_utils dedup + ADR-0009 | OPEN | CI ✅ all green |
| #43 | README/SPEC sync | OPEN | CI 运行中 |

## 技术债务清单（更新）

| # | 问题 | 影响 | 状态 |
|---|------|------|------|
| C1 | respx/httpx 本地 env 冲突 | pytest 无法本地运行 | ✅ 已修复（卸载 respx） |
| C2 | core/train_utils.py 重复 def | 死代码 | ✅ 已清理（PR#42） |
| C3 | api/health.py 缺 docstring | P3 | 未处理 |

## 下轮待办

1. [ ] **PR#40 merge**（等待 review）— v5→v6 升级 docs
2. [ ] **PR#41 merge**（等待 review）— main.py 数据集去重
3. [ ] **PR#42 merge**（CI all green ✅）— train_utils dedup + ADR-0009
4. [ ] **PR#43 merge**（CI 运行中）— README/SPEC sync
5. [ ] **v6 phases.md v6 DoD 填写**（等 PR#40 合并后做）— ADR-0009 P3 剩余部分
6. [ ] **api/health.py docstring**（P3）— 非阻塞

## ADR-0009 v6 DoD 进度

| # | DoD 项目 | 状态 |
|---|---------|------|
| 1 | respx/httpx 冲突修复 | ✅ 本地已修复 |
| 2 | core/train_utils.py dedup | ✅ PR#42 完成 |
| 3 | API contract test 增强 | 待做 |
| 4 | Release 自动化 | 待做 |
| 5 | README/SPEC.md sync | ✅ PR#43 完成 |